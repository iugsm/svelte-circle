{
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write ."
  },
  "type": "module",
  "packageManager": "pnpm@8.10.0",
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.0.12"
  }
}
